import { Model } from "../../../src/Model/Model";
import { Transports } from "../../../src/Transport/Transports";

export let sender = {
    send: (transportTypes: Transports, model: Model) => {

    }
};

export let modelBuilder = {
    build: (model: Model, dataLayerElement: any) => {

    }
};
